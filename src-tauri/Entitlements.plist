<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.device.camera</key>
  <true/>
  <key>com.apple.security.device.audio-input</key>
  <true/>
  <!-- Team-ID prefix hardcoded: Tauri's macOS bundler does not expand the
       Xcode $(AppIdentifierPrefix) build variable, so the literal macro string
       was an invalid keychain access group. That poisoned the app's default
       group and made every biometry keychain write fail with -34018, silently
       downgrading biometric unlock to a non-persistent in-memory fallback. -->
  <key>com.apple.security.keychain-access-groups</key>
  <array>
    <string>VLMNL3V44U.org.alexandria.node</string>
  </array>
</dict>
</plist>
